import CinematicExperience from "./cinematic/CinematicExperience";

/* feat/norse-scroll-cinema — the reinvented, scroll-cinema experience.
 * The previous portfolio (Navbar, HeroAtlas, Chapters, …) remains in
 * src/components and src/sections for content reuse and is preserved on `main`. */
export default function App() {
  return <CinematicExperience />;
}
